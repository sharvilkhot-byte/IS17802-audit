/**
 * UNCLINQ DESIGN SYSTEM — BARREL EXPORT
 *
 * Import everything from a single entry point:
 *
 *   import { color, shadow, Spinner, Card, ActionCard } from '../design-system'
 */

// Tokens
export * from './tokens'

// Components
export * from './components'

// Style guide page (dev use only — add /style-guide route in App.jsx)
export { default as StyleGuide } from './StyleGuide'
